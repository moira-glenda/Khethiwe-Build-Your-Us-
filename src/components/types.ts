export type Feature = {
  title: string;
  description: string;
  impact: string;
};

export type QuestionCategory = {
  name: string;
  tone: string;
  prompts: string[];
};

export type Testimonial = {
  quote: string;
  name: string;
  role: string;
};
